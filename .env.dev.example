# Copié automatiquement en .env.dev au premier `make dev`

POSTGRES_DB=combat
POSTGRES_USER=combat
POSTGRES_PASSWORD=combat

SECRET_KEY=dev-secret
ADMIN_PASSWORD=combat2026
SESSION_CODE=CHEFS
PUBLIC_URL=http://localhost:4200
CORS_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# Intervalle de scan des fichiers (ms) — rechargement auto sans surcharge
POLL_MS=2000
PORT_APP_DEV=4200
